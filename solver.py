from pathlib import Path
from parser import Rule, Letter, Truth, read_file, ParsedData
from typing import Dict, List, Optional, Set, Tuple

def solve(parsed: ParsedData) -> Dict[str, Truth]:
    
    return {}